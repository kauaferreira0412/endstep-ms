spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/endstep
    username: endstep
    password: endstep
  data:
    redis:
      host: redis
      port: 6379

endstep:
  scryfall:
    download-dir: /app/.data/scryfall
